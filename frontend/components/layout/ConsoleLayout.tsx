"use client";
import Link from "next/link";
import {Activity, BookOpen, Database, FileText, Gauge, Play, Settings, Terminal} from "lucide-react";
import {useLiveData} from "@/hooks/useLiveData";
const links=[["Dashboard","/dashboard",Gauge],["Manual Simulation","/manual",Terminal],["Automatic Simulation","/automatic",Play],["Cache Explorer","/cache",Database],["Analytics","/analytics",Activity],["Query Logs","/logs",FileText]] as const;
const item="focusable flex shrink-0 gap-3 items-center px-3 py-2 rounded hover:bg-[#10233a] text-sm";
export default function ConsoleLayout({children}:{children:React.ReactNode}){const live=useLiveData();return <div className="min-h-screen md:grid md:grid-cols-[242px_1fr]"><aside className="border-b md:border-b-0 md:border-r border-line bg-[#081424] p-4"><Link href="/" className="block font-semibold text-lg">MINI <span className="text-cyan">DNS</span><p className="label mt-1">Operations console</p></Link><nav className="mt-7 flex md:flex-col gap-1 overflow-x-auto">{links.map(([name,href,Icon])=><Link key={href} href={href} className={item}><Icon size={17}/>{name}</Link>)}<div className="hidden md:block border-t border-line mt-4 pt-4"><p className="label px-3 pb-2">Project</p><Link href="/project-guide" className={item}><BookOpen size={17}/>Project Guide</Link></div><Link href="/project-guide" className={`${item} md:hidden`}><BookOpen size={17}/>Project Guide</Link><Link href="/settings" className={item}><Settings size={17}/>Settings</Link></nav><div className="mt-8 panel p-3 text-xs"><p className="label">DNS Server</p><p className={live.connected?"text-hit mt-2":"text-miss mt-2"}>● {live.connected?"WEBSOCKET CONNECTED":"CONNECTING / OFFLINE"}</p><p className="muted mt-2">Upstream: configured backend</p><p className="muted">Port: 5353</p></div></aside><main><header className="h-16 flex items-center justify-between px-5 border-b border-line"><span className="font-medium">Mini DNS Server</span><div className="flex gap-3 text-xs"><span className={live.error?"text-miss":"text-hit"}>● API {live.error?"OFFLINE":"ONLINE"}</span><span className={live.connected?"text-hit":"text-miss"}>● WS {live.connected?"CONNECTED":"DISCONNECTED"}</span></div></header>{children}</main></div>}
